# Memket runtime dependencies
# Install: pip install -r requirements.txt

# Standard library only:
#   - arc_rpc.py          (stdlib JSON-RPC client for Arc)

# For the on-chain variant (Circle Gateway + Circle client):
requests>=2.30
web3>=6.0
eth-account>=0.10
hexbytes>=1.0

# For running the agent server / two-agent demo:
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
